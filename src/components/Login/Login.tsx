import React from "react";
// import { useParams } from "react-router-dom";

/**
 * This component checks if token is password protected and redirectes it to appropriate component
 */
const Login = () => {
  // const { token } = useParams();


  // return <h1>Login</h1>;
};

export default Login;
